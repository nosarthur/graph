package graph

// BreadthFirst implements breadth first search
func BreadthFirst(g Graph, src *Node, v interface{}) *Node {
	if src == nil {
		// get a random node from g as starting point
	}
	return nil
}
